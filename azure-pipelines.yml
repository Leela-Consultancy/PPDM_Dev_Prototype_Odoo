# Starter pipeline

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main  # Or your preferred branch

pool:
  vmImage: 'ubuntu-latest'

jobs:
- job: Build
  steps:
  - checkout: self

  - script: |
      # Set up your environment, install dependencies, etc.
      # For example, install Odoo dependencies.
          sudo apt-get install -y python3-pip
          sudo apt-get install -y zip
    displayName: 'Set up Environment'

  - task: ArchiveFiles@2
    inputs:
      rootFolderOrFile: '$(Build.SourcesDirectory)/Leela-Consultancy/PPDM_Dev_Prototype_Odoo/addons/indika_module'
      includeRootFolder: true
      archiveType: 'zip'
      archiveFile: '$(Build.ArtifactStagingDirectory)/indika_module.zip'
      replaceExistingArchive: true

  # - task: PublishBuildArtifacts@1
  #   inputs:
  #     PathtoPublish: '$(Build.SourcesDirectory)'
  #     ArtifactName: 'indika-artifact'
  #     publishLocation: 'Container'
  #   displayName: 'Publish Artifact'
